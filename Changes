

V2.0
----
Removed method _format_to_str(). Instead I'm using pack() to format the output.
This is done to to some wierd problems I encountered when filtering my own output
(such as dbh disconnection in every call to debug_message().
